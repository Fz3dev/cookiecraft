/**
 * Built-in translations for supported languages
 * Users can override any string via config.translations
 */
import { Translation } from '../types';
export declare const builtInTranslations: Record<string, Translation>;
