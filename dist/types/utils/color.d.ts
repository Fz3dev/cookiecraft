/**
 * Adjust a hex color brightness by a percentage
 * Negative = darker, positive = lighter
 */
export declare function adjustColorBrightness(color: string, percent: number): string;
/**
 * Build inline CSS custom properties for a primary color
 */
export declare function buildColorStyle(safeColor: string): string;
